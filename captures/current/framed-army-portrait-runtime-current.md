# Native portrait selection at1024×768

**PASS — bounded controlled native input and software layout**, recorded at
2026-09-06T10:20:13.7382740Z. The first portrait of own army3 toggled0→1→0
through the actual423860 handler on candidate `9bc99ba5…508c53b5` in the
`-framed-modalcanvas-army-validation` stage.

Both native left queries returned1; right queries returned0. Native XOR423952
changed only the first selection flag. Both complete draw/map sequences,
controlled release followed by actual zero button queries, secondary-map
return0, and the true native return pass the source-bound trace. No movement
route was entered. All three actual surface headers, compiled commands,
original/save/candidate and retained process cleanup are bound.

Every screenshot passes all four frame edges and all six bottom-right action
cells. The selected portrait has53 exact native badge pixels; seven native
map-marker masks explain the other1,190 changed pixels. No changed pixels are
unexplained. The second click restores all786,432 original indexed pixels.
Map-marker blend colors have not been independently checked.

The [manifest](framed-army-portrait-runtime-current.json) links the full trace,
screenshots, source-marker audit and cleanup receipts. It retains the initial
parser's incorrect startup-order failure and the disproved badge-only pixel
hypothesis without overwriting either report.

This is controlled hidden input, not ordinary map dispatch or manual/visible
proof. Other portraits, resolutions, held-button behavior and promotion remain
separate requirements. The original executable, candidate, work assets and
protected stable stage are unchanged.
