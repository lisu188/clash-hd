# Owned castle canvas: actual runtime checkpoint

The corrected 1024x768 candidate has one coherent **castle overview** software
capture. Its bounded runtime/trace, native/physical capture binding and exact
centered mirror pass. All 307,200 native pixels match; all 479,232 margin pixels
are index 0, whose saved palette color is RGB(6,5,5).

The ordinary-map frame/footer comparison fails and its action bar is 0/6.
Those are nongating measurements on centered castle artwork: the screenshot
shows the castle back button and bottom status strip, with no manual control
proof. Native modal exit/free and return-to-map remain untested.

The subsequent constructed-hospital attempt **fails before capture**: its
still-armed observer rejects an additional normal overview artwork load.
The first overview protocol disabled that observer after one load, so its
successful screenshot does not individually prove every artwork-load call.
A source-bound expanded sequence is being prepared without changing the game
candidate. Earlier striped captures and the first invalid-PE attempt remain failed.

[Exact source/candidate/artifact hashes, saved protocol sources and cleanup receipts](framed-modal-canvas-runtime-current.json).

Both owned debugger/game pairs were stopped and their handles closed. The
original executable and all isolated workdir files remain unchanged. The
stable stage is unchanged; no visible/manual input or promotion is established.
