# Hidden day-transition control — executed, continuity gate failed

Update on 2026-09-05: the user approved this prepared run with
"I approve hidden controle run"; the [approval receipt](continuity-day-control-approval-current.json)
binds the unchanged plan. Run `cdb-surface-dump-20260905-130340` completed,
produced an 800×600 surface and passed all 166 byte checks. Exact recorded
game/debugger PIDs 33576/37368 were absent after the harness completed.
Its [strict continuity result](continuity-day-v5-control-current.json) remains
failed: a repeated mouse-call trace and an incorrect parser mode expectation
prevented acceptance. The raw log records native day `1→2`, one left and one
right release override, the release return, the next-player return and the
post-day surface. No banner override was used. These observations remain
controlled native-call evidence, not manual input or promotion proof.

The following is the historical preparation description; its unapproved
wording reflects the state before the later user message and execution.

The [execution plan](continuity-day-control-run-plan-current.json) records the
exact command and source hashes for a maximum **180-second** hidden CDB run.
Its private assets are prepared under
`C:\ClashTests\hd-completion\continuity-daydiag-combined-v5-control-20260905-125300`.
The probe SHA-256 is
`018EDEEF8B977718770139C9E5D28021D9DD16EE5EB31EF4778DE2FC2690875B`.
No execution or approval has occurred for this run.

The harness would build the known 800×600 combined validation candidate from
the protected original, checking its SHA and old bytes. Expected output SHA:
`0A75CF35F42EFE1E44FBA1AC4031EAAE5323CDFF51AA19D261C36C5BB84691C2`.
It would use x86 CDB on a hidden desktop and the non-presenting proxy, retain
raw evidence outside Git, and stop its own processes. The ongoing soak has
different directories and processes and is not part of this command.

The earlier observation reached native day increment `1→2` but stopped at
eight held-left-button release tests while DirectInput reads failed. This
control would make at most five native next-player calls. It could override
one banner query after eight zero results, then at most one release query for
each button at the eighth release-loop iteration. Each override logs the
original result and actual observation count. No day/player field or OS input
is written; the game may update only its private settings or saves.

A result would be **forced native-call/control evidence**, never manual input,
visible composition, sustained gameplay, or promotion proof. Missing returns,
surface evidence, or cleanup would remain failures. Fifteen focused probe
checks pass, including exact native bytes and bounded-control negatives.

Automatic approval review rejected launching this control, citing the earlier
Astra preparation plan's no-runtime scope and lack of later authorization for
this exact experiment. The risk is execution of the local game/debugger with
controlled query results and private runtime-file writes. Approval is needed
before executing this command; file-only preparation has completed.
