# Corrected hidden day-transition control — executed, strict gate failed

The user's subsequent **"Approve debugger runs"** message authorized this run.
The [approval receipt](continuity-day-v6-control-approval-current.json) binds
the original preparation below. Run `20260905-135344` completed with an outer
surface pass and verified cleanup, but the [strict result](continuity-day-v6-control-current.md)
fails on a duplicate input-return marker during advance 2. The preparation
text below is historical and is not the current execution status.

## Historical preparation

The [v6 execution plan](continuity-day-v6-control-run-plan-current.json) records
the exact command, source hashes, candidate identity, and 50 private starting
asset/settings files. It requests one additional run, bounded to **180 seconds**,
under `C:\ClashTests\hd-completion\continuity-daydiag-combined-v6-control-20260905-131500`.

The approved v5 run finished with a post-day surface and verified cleanup. Its
[original report](continuity-day-v5-control-current.json) remains preserved;
the [corrected-parser recheck](continuity-day-v5-control-recheck-current.json)
still fails on an overlapping input call. The parser correction handles the
documented distinction between banner and release overrides. It does not
remove, deduplicate, or excuse the malformed trace.

This v6 producer changes only diagnostic version/comment and trace closure:
after the exact verified release return, it disables input breakpoints 87-91
and records an explicit end marker before subsequent native work. It retains
all native-byte checks, saved-stack checks, at most five next-player calls,
one banner override after eight zero tests, and at most one release-query
override per button at release-loop iteration eight. Sixteen producer fixtures
and twenty-five parser fixtures pass, including wrong-stack, wrong-mode,
repeated-override, missing trace-end, and late-input rejection.

The harness would build the same 800×600 combined candidate from the known
original and verify its 166 patch records. Expected candidate SHA-256:
`0A75CF35F42EFE1E44FBA1AC4031EAAE5323CDFF51AA19D261C36C5BB84691C2`.
It uses x86 CDB on a hidden desktop and the non-presenting proxy. It can write
only the prepared private game state and runtime artifacts through this path,
then stop its own processes. The ongoing two-hour soak uses separate files
and processes.

There is no visible game session, OS input injection, or live screen capture.
A passing result would establish only bounded forced-native-call continuity
with explicitly counted query-result overrides. It would not establish manual
input, sustained campaign play, visible composition, or promotion.

The user's earlier approval covered the executed v5 plan. This separate rerun
is prepared for review; no v6 approval or execution is recorded yet.
