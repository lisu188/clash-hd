# September 24 battle-input source bindings

The battle-only cursor-bounds and relative-input repair adds seven lines to the canonical patcher's stage dispatcher. The existing 3,048 lines retain their exact bytes. Its source identity changes from `05f31359f93a0eb0b319679ee524b21c05cd3e86e485b7ebb92afc8e6da29f31` to `38021e9a4d21bc9a8bf0c2f9b66379595509446b5177d6f91bab6f677b5e8106`. The dispatcher replaces only the two inherited input hooks for the isolated 1280x720 battle stage.

Thirty-six current producer and consumer files now bind that reviewed source through 54 SHA literal substitutions. Every old pin matched its dependency's actual source identity before this change. The changed files retain their algorithms, command protocols, tests and guard logic. This includes three transitive trace-source identity entries in the Python runtime safety guard; their exemption conditions were not changed. Historical reports and the foundation import manifest retain their pre-edit checkout hashes. Existing Git line-ending conversion means some historical report checkout hashes differ from their Git blob hashes; neither representation was rewritten.

The frozen legacy table, stage groups and default stage are unchanged. Comparing the previous and current dispatcher across six resolution fixtures preserves all 147 accepted selections and 225 rejections outside the changed battle stage. The framed recipe's 11 tests and the Python runtime safety guard fixtures pass. Independent native x86 checks also pass 675 cursor-switch/clamp scenarios and 629 relative-update/recenter scenarios; those checks read the verified original executable and emulate instructions without launching the game.

A fresh source-only Windows Git checkout with `core.autocrlf=true` reproduces all 37 reviewed source identities and 54 updated dependency pins. The current `.gitattributes` already preserves their bytes: 35 paths have `text=unset`, and the two newer leaf producers explicitly use LF. Its framed candidate preflight verifies 14 source dependencies. In-memory reconstruction yields battle candidate `99d92ec7c8f81debf60321dcc5c1b5872c96e3c485fa2bdd7d9332287b3c7e87` with 283 patches; no candidate file or game asset was copied into the source checkout.

Eleven direct source preflights pass. Five historical framed-army probe preflights remain blocked by their pre-existing renderer pin: expected `1cd3103c434a898e0104b519932f1ce30be259888885f251422a8b71d3a9d02f`, while the unchanged current renderer is `12685fdfde4972e0f5f9c3d5678fd159fff965bd9ddd7642cb3718c8530296cf`. This unrelated mismatch was preserved, so this report does not claim all historical source checks pass.

The exact old/new identities, dependency substitutions, checkout location, focused checks and preserved historical hashes are recorded in [the source-binding manifest](battle_hd_source_binding_20260924.json). This work is repository and emulator validation only. The approved visible run used the earlier `7d04fe90…` candidate; it does not validate the new input repair, and neither candidate is promoted.
